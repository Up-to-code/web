## Broker Zone Server Functions

This folder owns Broker application logic on the Next.js server.

- resolve the current broker session
- validate Broker-facing payloads
- enforce owner checks before writes
- delegate persistence to Convex internal CRUD primitives
