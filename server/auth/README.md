## Auth Layer

This folder owns web-session resolution for the Next.js backend gateway.

- Resolve the current Convex-backed session.
- Enrich it with profile and role data.
- Expose one shared session context for routes and server-side page loaders.
