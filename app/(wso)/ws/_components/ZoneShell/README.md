# ZoneShell

Dedicated full-screen shell primitives for business-zone routes under `/ws`.

## Files
- `index.tsx`: thin orchestrator for the full-screen zone shell.
- `ZoneSidebar.tsx`: zone-specific sidebar with back-to-workspace and local links.
