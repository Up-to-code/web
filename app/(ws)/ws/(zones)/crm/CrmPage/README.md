# CrmPage

Focused read-only CRM view for the workspace CRM zone.

## Responsibilities
- Render the initial pipeline and deals overview for broker and developer roles.
- Keep deal-table and stage-summary presentation inside the CRM zone folder.
- Reserve deal detail and property-scoped subviews for future nested routes.

## Structure
- `index.tsx`: page orchestrator.
