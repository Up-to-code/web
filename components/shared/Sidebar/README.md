# Sidebar

Shared workspace sidebar primitives used by the `/ws` shell.

## Files
- `index.tsx`: thin orchestrator that chooses the correct wrapper for desktop or drawer usage.
- `SidebarContent.tsx`: shared role-aware navigation and identity block markup.
- `types.ts`: shared prop contracts for sidebar variants.
